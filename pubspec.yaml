name: riba
description: Cross-platform mobile client for MangaDex
version: 1.0.0+1
publish_to: "none"

environment:
    sdk: ">=2.18.6 <3.0.0"

dependencies:
    flutter:
        sdk: flutter

    animations: ^2.0.7
    dynamic_color: ^1.5.4

    logging: ^1.1.0

    http: ^0.13.5
    path_provider: ^2.0.11

    isar: ^3.0.5
    isar_flutter_libs: ^3.0.5

    hive: ^2.2.3
    hive_flutter: ^1.1.0

    package_info_plus: ^3.0.2
    url_launcher: ^6.1.9
    google_fonts: ^4.0.3
    fl_chart: ^0.61.0

dev_dependencies:
    flutter_test:
        sdk: flutter

    flutter_lints: ^2.0.0
    build_runner: ^2.3.3

    isar_generator: ^3.0.5
    hive_generator: ^2.0.0

flutter:
    uses-material-design: true
